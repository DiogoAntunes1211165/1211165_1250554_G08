version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8081:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=bootstrap,sqlServer

      # SQL Server
      - DB_HOST=sqlserverg08.database.windows.net
      - DB_PORT=1433
      - DB_NAME=psoftg1
      - DB_USER=grupo08
      - DB_PASSWORD=Grupo8arqsoft_

      # MongoDB remoto (Atlas)
      - MONGO_HOST=cluster.7xtesex.mongodb.net
      - MONGO_DB=1211165_1250554_G08
      - MONGO_USER=1211165_1250554
      - MONGO_PASSWORD=tunestiago008
